# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2025 The Linux Foundation

# Example secrets file for Nektos/Act local testing
# Copy this file to .secrets and fill in your actual values
#
# Usage: cp .secrets.example .secrets
# Then edit .secrets with your actual secret values
#
# IMPORTANT: .secrets is in .gitignore - never commit secrets!

# GitHub Personal Access Token (for API rate limiting)
# Create at: https://github.com/settings/tokens
# Required scopes: public_repo (for public repositories)
GITHUB_TOKEN=ghp_your_token_here

# GPG Private Key (for signature verification tests)
# Export your test GPG key with:
#   gpg --armor --export-secret-keys test@tag-validate-action.local
# Then paste the entire output (including BEGIN/END lines) below
# Leave empty if you don't have a test GPG key - tests will adapt
GPG_PRIVATE_KEY=
